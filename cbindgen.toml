language = "C"
cpp_compat = true
include_guard = "MPV_SOCKET_H"
include_version = true
autogen_warning = "/* Warning, this file is autogenerated by cbindgen. Don't modify this manually. */"
no_includes = true
sys_includes = ["stdint.h", "stddef.h"]

[parse.expand]
crates = ["mpv-socket"]
features = ["ffi"]

[defines]
# This will silence ```WARN: Missing `[defines]` entry for `feature = "ffi"` in cbindgen config.```
# but generates unwanted `#if defined(MPV_SOCKET_H)` blocks.
# Ignoring the warning for now...
#"feature = ffi" = "MPV_SOCKET_H"
